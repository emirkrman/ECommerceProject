@model List<ECommerceProject.Entity.Concrete.Category>

<h2>Categories</h2>

<a asp-action="Create" class="btn btn-success mb-2">New Category</a>

<table class="table">
    <thead>
        <tr>
            <th>Name</th>
            <th>Description</th>
            <th>Active</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var category in Model.Where(c => c.ParentCategoryId == null))
        {
            <tr>
                <td><strong>@category.Name</strong></td>
                <td>@category.Description</td>
                <td>@category.IsActive</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@category.Id" class="btn btn-warning btn-sm">Edit</a>
                    <a asp-action="Delete" asp-route-id="@category.Id" class="btn btn-danger btn-sm">Delete</a>
                </td>
            </tr>

            @foreach (var subCategory in Model.Where(c => c.ParentCategoryId == category.Id))
            {
                <tr>
                    <td class="ps-5">↳ @subCategory.Name</td>
                    <td>@subCategory.Description</td>
                    <td>@subCategory.IsActive</td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@subCategory.Id" class="btn btn-warning btn-sm">Edit</a>
                        <a asp-action="Delete" asp-route-id="@subCategory.Id" class="btn btn-danger btn-sm">Delete</a>
                    </td>
                </tr>
            }
        }
    </tbody>
</table>